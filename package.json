{
  "name": "sigfrid-von-shrink",
  "version": "2.0.0",
  "type": "module",
  "main": "functions/api/chat.js",
  "scripts": {
    "build": "esbuild worker.js --bundle --outfile=dist/worker.js --format=esm"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
